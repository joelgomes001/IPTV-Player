rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read/write rules for channels, analytics telemetry, and user preferences
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
